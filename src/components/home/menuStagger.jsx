import { useGSAP } from "@gsap/react";
import gsap from "gsap";

const MenuStagger = () => {
    useGSAP(() => {
        
    },[])
    return (
        <div>
            
        </div>
    )
}

export default MenuStagger